---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gym-coach-app-api/package.json
  - gym-coach-app-api/tsconfig.json
  - gym-coach-app-api/.env.example
  - gym-coach-app-api/src/index.ts
  - gym-coach-app-api/src/middleware/errorHandler.ts
  - gym-coach-app-api/src/middleware/validateBody.ts
  - gym-coach-app-api/railway.json
  - gym-coach-app-api/Procfile
autonomous: false
requirements:
  - BACK-01
  - BACK-03

must_haves:
  truths:
    - The Express backend starts without errors and responds to GET /health with 200 + JSON
    - The backend is deployed to Railway and reachable from an external HTTP client
    - No secrets committed to git -- only .env.example is tracked
    - TypeScript compiles without errors via npm run build
  artifacts:
    - path: gym-coach-app-api/src/index.ts
      provides: Express app entry point with CORS helmet JSON middleware mounted
    - path: gym-coach-app-api/src/middleware/errorHandler.ts
      provides: Global Express 5 async error handler
    - path: gym-coach-app-api/.env.example
      provides: Template listing all required env vars
    - path: gym-coach-app-api/railway.json
      provides: Railway deploy config with health check path
  key_links:
    - from: gym-coach-app-api/src/index.ts
      to: Railway public internet
      via: PORT from process.env, GET /health returns 200
---

<objective>
Bootstrap the Node/Express TypeScript backend and deploy it to Railway so subsequent plans have a live server to build on.

Purpose: Every backend plan depends on the Express app existing and deployed. This plan creates the skeleton -- CORS, helmet, JSON parsing, health check, error handler. Nothing more.

Output: Deployed Express 5 + TypeScript backend on Railway, GET /health returns 200.
</objective>

<execution_context>
@C:/Users/cleme/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/cleme/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type=auto>
  <name>Task 1: Scaffold Express 5 + TypeScript backend</name>
  <files>gym-coach-app-api/package.json, gym-coach-app-api/tsconfig.json, gym-coach-app-api/.env.example, gym-coach-app-api/.gitignore, gym-coach-app-api/src/index.ts, gym-coach-app-api/src/middleware/errorHandler.ts, gym-coach-app-api/src/middleware/validateBody.ts</files>
  <action>
    Create gym-coach-app-api/ as a sibling of the mobile app (NOT nested inside it).
    Location: C:\Users\cleme\Documents\AI-ClaudeOpus\projets\gym-coach-app-api
    package.json:
      name: gym-coach-app-api, engines: {node: >=20.0.0}
      scripts: dev: nodemon --exec ts-node src/index.ts, build: tsc, start: node dist/index.js
      dependencies: express@^5, helmet@^8, cors@^2.8.5, dotenv@^16
      devDependencies: typescript@^5, ts-node@^10.9, nodemon@^3,
        @types/express@^5, @types/node@^20, @types/cors@^2.8

    tsconfig.json: target ES2022, module CommonJS, outDir dist, rootDir src,
      strict true, esModuleInterop true, skipLibCheck true, resolveJsonModule true

    src/index.ts:
      - require(dotenv).config() at top
      - app.use(helmet(), cors({origin: process.env.CORS_ORIGIN||*}), express.json())
      - GET /health -> {status:ok, ts: new Date().toISOString()}
      - app.use(errorHandler) from ./middleware/errorHandler
      - app.listen(process.env.PORT || 3000)
      - export {app}

    src/middleware/errorHandler.ts:
      export const errorHandler: ErrorRequestHandler = (err, _req, res, _next) => {
        res.status((err as any).status ?? 500).json({error: err.message ?? Internal server error})
      }

    src/middleware/validateBody.ts (placeholder -- completed in Plan 03):
      export function validateBody(): RequestHandler { return (_req, _res, next) => next() }

    .env.example: PORT=3000, CORS_ORIGIN=*, SUPABASE_URL=, SUPABASE_SERVICE_ROLE_KEY=,
      SUPABASE_JWT_SECRET=, OPENROUTER_API_KEY=, GOOGLE_API_KEY=, OPENAI_API_KEY=

    .gitignore: node_modules/, dist/, .env
  </action>
  <verify>
    <automated>cd gym-coach-app-api and npm install and npm run build and echo BUILD_OK</automated>
  </verify>
  <done>npm run build exits 0, zero TypeScript errors. Server starts on port 3000.</done>
</task>

<task type=auto>
  <name>Task 2: Railway deployment configuration</name>
  <files>gym-coach-app-api/railway.json, gym-coach-app-api/Procfile</files>
  <action>
    railway.json (valid JSON):
    {
      build: {builder: NIXPACKS, buildCommand: npm run build},
      deploy: {
        startCommand: npm start,
        healthcheckPath: /health,
        healthcheckTimeout: 30,
        restartPolicyType: ON_FAILURE,
        restartPolicyMaxRetries: 3
      }
    }

    Procfile: web: node dist/index.js

    Railway auto-injects PORT. Do NOT hardcode it.
  </action>
  <verify>
    <automated>npm run build exits 0 and railway.json exists</automated>
  </verify>
  <done>railway.json valid JSON. Procfile exists. Build passes.</done>
</task>

<task type=checkpoint:human-verify gate=blocking>
  <what-built>Express 5 TypeScript backend with GET /health and Railway config</what-built>
  <how-to-verify>
    1. Push gym-coach-app-api/ to GitHub
    2. railway.app -> New Project -> Deploy from GitHub
    3. If monorepo: set Root Directory to gym-coach-app-api
    4. Wait for build (watch logs -- TypeScript compile then server start)
    5. Generate Domain to get public URL
    6. GET https://your-app.railway.app/health
    7. Expected: {status:ok,ts:2026-...}
    8. Confirm .env NOT in GitHub (only .env.example visible)
  </how-to-verify>
  <resume-signal>Type deployed: https://your-url.railway.app or describe errors</resume-signal>
</task>

</tasks>

<verification>
- GET /health at Railway URL returns 200 + {status:ok}
- npm run build exits 0
- .env not in git
</verification>

<success_criteria>
Deployed Express 5 TypeScript backend on Railway. /health returns 200. No secrets in git.
</success_criteria>

<output>
After completion, create .planning/phases/01-backend-foundation/01-01-SUMMARY.md with:
Railway URL, Express version, folder structure, any deviations.
</output>