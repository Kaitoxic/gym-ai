---
phase: 01-backend-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - gym-coach-app-api/src/services/db.ts
  - gym-coach-app-api/src/routes/healthDb.ts
  - supabase/migrations/001_initial_schema.sql
autonomous: false
requirements:
  - BACK-02
  - BACK-03

must_haves:
  truths:
    - Supabase project exists with Auth enabled
    - users table exists with Row Level Security enforced
    - user_quotas table exists for per-user AI call tracking
    - Backend connects to Supabase and GET /health/db returns {db:ok}
  artifacts:
    - path: gym-coach-app-api/src/services/db.ts
      provides: Supabase service-role client singleton exported as supabase
      exports: [supabase]
    - path: supabase/migrations/001_initial_schema.sql
      provides: SQL for users + user_quotas tables with RLS policies
      contains: CREATE TABLE users, CREATE TABLE user_quotas, ENABLE ROW LEVEL SECURITY
    - path: gym-coach-app-api/src/routes/healthDb.ts
      provides: GET /health/db route confirming Supabase connectivity
  key_links:
    - from: gym-coach-app-api/src/services/db.ts
      to: Supabase Cloud Postgres
      via: @supabase/supabase-js createClient with service role key
    - from: supabase/migrations/001_initial_schema.sql
      to: Supabase Postgres public schema
      via: Supabase SQL Editor
---

<objective>
Create the Supabase project, define the initial database schema (users + quota tracking), and wire the backend to Supabase using the service role key.

Purpose: BACK-03 requires cloud storage. BACK-02 requires a quota tracking table. This plan creates the schema foundation that every subsequent phase writes to.

Output: Live Supabase project with users and user_quotas tables + RLS, Supabase client service, and GET /health/db confirming the database connection.
</objective>

<execution_context>
@C:/Users/cleme/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/cleme/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md

<interfaces>
From 01-01: Add new routes in src/routes/, mount in src/index.ts before errorHandler.
Pattern: import router from ./routes/X; app.use(/path, router)
</interfaces>
</context>

<tasks>

<task type=checkpoint:human-action gate=blocking>
  <what-built>N/A -- Supabase project creation requires dashboard access</what-built>
  <how-to-verify>
    1. Go to supabase.com -> New Project, name: gym-coach-app
    2. Set strong database password (save it)
    3. Choose region closest to Railway deployment
    4. Wait ~2 min for provisioning
    5. Authentication -> Providers: Enable Email (confirm email OFF for dev)
    6. Settings -> API: Copy Project URL (SUPABASE_URL), service_role key (SUPABASE_SERVICE_ROLE_KEY), JWT Secret (SUPABASE_JWT_SECRET)
    7. Add these 3 values to Railway environment variables for gym-coach-app-api
    8. Create local gym-coach-app-api/.env with these values (already gitignored)
    9. Run SQL in Supabase SQL Editor:

    CREATE EXTENSION IF NOT EXISTS pgcrypto;

    CREATE TABLE IF NOT EXISTS public.users (
      id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
      goal TEXT CHECK (goal IN (muscle_gain, fat_loss, endurance, general_fitness)),
      fitness_level TEXT CHECK (fitness_level IN (beginner, intermediate, advanced)),
      injury_notes TEXT,
      available_days INT[] DEFAULT {},
      equipment TEXT[] DEFAULT {},
      body_weight FLOAT,
      body_height FLOAT,
      body_age INT,
      ai_provider TEXT DEFAULT openrouter CHECK (ai_provider IN (openrouter, gemini, openai)),
      ai_model TEXT DEFAULT openai/gpt-4o,
      onboarding_done BOOLEAN DEFAULT FALSE,
      created_at TIMESTAMPTZ DEFAULT NOW(),
      updated_at TIMESTAMPTZ DEFAULT NOW()
    );
    ALTER TABLE public.users ENABLE ROW LEVEL SECURITY;
    CREATE POLICY users_self_access ON public.users USING (id = auth.uid()) WITH CHECK (id = auth.uid());

    CREATE TABLE IF NOT EXISTS public.user_quotas (
      id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
      date DATE NOT NULL DEFAULT CURRENT_DATE,
      ai_calls INT NOT NULL DEFAULT 0,
      UNIQUE (user_id, date)
    );
    ALTER TABLE public.user_quotas ENABLE ROW LEVEL SECURITY;
    CREATE POLICY quotas_self_read ON public.user_quotas FOR SELECT USING (user_id = auth.uid());

    10. Verify both tables appear in Table Editor with RLS ON
  </how-to-verify>
  <resume-signal>Type supabase ready when provisioned and env vars set, or describe issues</resume-signal>
</task>

<task type=auto>
  <name>Task 2: Supabase backend client + DB health check route</name>
  <files>gym-coach-app-api/src/services/db.ts, gym-coach-app-api/src/routes/healthDb.ts, gym-coach-app-api/src/index.ts</files>
  <action>
    Install: cd gym-coach-app-api && npm install @supabase/supabase-js

    src/services/db.ts:
      import { createClient } from @supabase/supabase-js
      const supabaseUrl = process.env.SUPABASE_URL!
      const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY!
      if (!supabaseUrl || !serviceRoleKey) throw new Error(SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set)
      export const supabase = createClient(supabaseUrl, serviceRoleKey, { auth: { persistSession: false } })
      // Service role client -- bypasses RLS. NEVER expose to clients.

    src/routes/healthDb.ts:
      import { Router } from express
      import { supabase } from ../services/db
      const router = Router()
      router.get(/, async (req, res) => {
        const { error } = await supabase.from(users).select(id).limit(1)
        if (error) { res.status(503).json({ db: error, message: error.message }); return }
        res.json({ db: ok })
      })
      export default router

    Update src/index.ts:
      Add: import healthDbRouter from ./routes/healthDb
      Add: app.use(/health/db, healthDbRouter)  // before errorHandler

    Also save the migration SQL to supabase/migrations/001_initial_schema.sql in the repo root
    for documentation purposes (already run in Supabase dashboard).
  </action>
  <verify>
    <automated>cd gym-coach-app-api && npm run build && echo DB_ROUTE_BUILD_OK</automated>
  </verify>
  <done>npm run build exits 0. After deploy, GET /health/db on Railway URL returns {db:ok}. Tables visible in Supabase Table Editor with RLS ON.</done>
</task>

</tasks>

<verification>
- GET https://your-app.railway.app/health/db returns {db:ok}
- Supabase Table Editor shows public.users and public.user_quotas with RLS ON
- SUPABASE_SERVICE_ROLE_KEY only in Railway env vars and local .env -- not committed
- npm run build exits 0
</verification>

<success_criteria>
Supabase project live with users and user_quotas tables + RLS. Backend connects and confirms via GET /health/db. Schema foundation ready for all future phases.
</success_criteria>

<output>
After completion, create .planning/phases/01-backend-foundation/01-02-SUMMARY.md with:
Supabase project URL, tables created, RLS status, whether OAuth configured, any deviations.
</output>